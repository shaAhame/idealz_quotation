# server/.env.example
# Copy to .env and fill in your values

# Database (Railway PostgreSQL)
DATABASE_URL="postgresql://postgres:paxsybclrSECcOUlDbvPVDBKBNAhuXym@kodama.proxy.rlwy.net:44504/railway"

# JWT
JWT_SECRET="change-this-to-a-long-random-secret-string-at-least-32-chars"

# App URL (your Railway domain)
APP_URL="https://your-app.up.railway.app"

# Client URL (same if monorepo, or separate Vercel URL)
CLIENT_URL="https://your-app.up.railway.app"

# SMTP — use Gmail App Password (free), or Brevo/Mailersend free tier
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="info@idealz.lk"
SMTP_PASS="ibxn smjd lktr exlw"

# Environment
NODE_ENV="production"
PORT="3001"
